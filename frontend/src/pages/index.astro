---
import Layout from "../layouts/Layout.astro";

import GlobalMap from "@/components/maps/GlobalMap.tsx";
import ArrivalBoard from "@/components/transit/ArrivalBoard.tsx";

import "@/styles/globals.css";
---

<Layout title="Clairvoyance">
    <GlobalMap client:only  mode="main" transition:persist transition:name="global-map" />
    
	<ArrivalBoard client:load lat={0} lng={0}/>
</Layout>
